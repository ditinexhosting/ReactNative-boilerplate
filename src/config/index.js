import ENVDATA from "../../env.json"
const {URL, ENV} = ENVDATA

// Constant Values

export const SESION = {};
export const API_URL = URL;
export const API_STORAGE = URL;
export const DOMAIN = '';
export const ENVIRONMENT = ENV;
